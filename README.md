# Lab 1 - Matrix Multiplication
This lab compared matrix multiplication in GO between sequential and concurrent implementations, the latter of which utilize goroutines for each corresponding row and column of matrices A and B.

Different numbers of goroutines were tested to characterize the effect on speed.

## Usage

```bash
cd lab1
go run .
```
